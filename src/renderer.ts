/*
"use strict";

const {dialog} = require('electron');

const myNotification = new Notification('Title', {
    body: 'Notification from the Renderer process'
})

myNotification.onclick = () => {
    dialog.showMessageBox('Notification clicked');
}*/
